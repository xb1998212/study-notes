# Git 常用命令速查表

## 配置
- `git config --global user.name "用户名"`
- `git config --global user.email "邮箱"`

## 基础操作
- `git clone <仓库地址>` —— 下载仓库到本地
- `git add .` —— 暂存所有改动
- `git commit -m "备注"` —— 提交
- `git push` —— 推送到云端
- `git pull` —— 拉取最新代码
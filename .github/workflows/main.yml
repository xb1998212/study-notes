name: CI
on:
  push:
    branches: [ main ]
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: 输出问候
        run: echo "你好，GitHub Actions！"
      - name: 列出根目录文件
        run: ls -la
        
